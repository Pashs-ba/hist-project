import {Quest} from "../types/Quest.ts";

export function GetQuests() {
    return new Promise<Quest>(async (resolve, reject) => {
        var quest = new Quest()
        Quest
        resolve()
    })
}